---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: jenkins
  namespace: podinfo #jenkins
spec:
  chart:
    spec:
      version: ">=1.0.0-alpha"
  test:
    enable: false
